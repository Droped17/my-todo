export type TodoProps = {
  id: number;
  title: string;
  body: string;
};

export type Post = {
  userId: number;
  id: number;
  title: string;
  body: string;
};

export type ModalType = {
  isOpen: boolean;
};
